<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.germsoft.bizqrd">

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/BizQRd"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">

        <activity android:name="com.germsoft.bizqrd.activities.SettingsActivity"
            android:label="Settings"
            android:parentActivityName="com.germsoft.bizqrd.activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.germsoft.bizqrd.activities.MainActivity"/>
        </activity>
        <activity android:name="com.germsoft.bizqrd.activities.HelpActivity"
            android:label="Help"
            android:parentActivityName="com.germsoft.bizqrd.activities.MainActivity">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.germsoft.bizqrd.activities.MainActivity"/>
        </activity>
        <activity android:name="com.germsoft.bizqrd.activities.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>